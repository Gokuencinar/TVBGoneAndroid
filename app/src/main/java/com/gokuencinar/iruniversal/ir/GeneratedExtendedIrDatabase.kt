package com.gokuencinar.iruniversal.ir

/**
 * Placeholder for local builds.
 *
 * GitHub Actions regenerates this file from the pinned community and official
 * Flipper IR databases before producing release APKs.
 */
object GeneratedExtendedIrDatabase {
    val televisionPowerExtras: List<IrCode> = emptyList()
    val projectorPowerExtras: List<IrCode> = emptyList()
    val televisionLibrary: List<IrCode> = emptyList()
    val airConditionerLibrary: List<IrCode> = emptyList()
    val projectorLibrary: List<IrCode> = emptyList()
}
